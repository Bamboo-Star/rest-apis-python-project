# contains variables we want flask to use when we start it up
# specify the app script
FLASK_APP=app
# in this mode, if we change our code, the app will restart itself
# FLASK_ENV=development
FLASK_DEBUG=1