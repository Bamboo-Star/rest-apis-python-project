# contains every python library this project depends on
# if add new packages, need to rebuild docker to install new packages
flask==2.1.3
flask-smorest
python-dotenv
sqlalchemy==1.4
flask-sqlalchemy
flask-jwt-extended
passlib
flask-migrate